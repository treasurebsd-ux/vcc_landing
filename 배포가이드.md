# LEGACY VCC 랜딩페이지 배포 가이드

폴더 구성: `index.html` + `images/` (사진 27장). 이 폴더 전체를 그대로 사용합니다.

## 1. GitHub에 올리기

1. github.com에서 새 저장소(Repository)를 생성합니다. (예: `legacy-vcc-landing`, Public 또는 Private 모두 가능)
2. 이 폴더(`index.html`, `images/` 포함)를 저장소에 업로드합니다.
   - 웹 브라우저에서: 저장소 페이지 → "Add file" → "Upload files" → 폴더 안의 파일을 전부 드래그 앤 드롭 → Commit.
   - 또는 터미널에서:
     ```
     git init
     git add .
     git commit -m "LEGACY VCC 랜딩페이지"
     git branch -M main
     git remote add origin https://github.com/아이디/legacy-vcc-landing.git
     git push -u origin main
     ```

## 2. Vercel로 배포하기

1. vercel.com에 GitHub 계정으로 로그인합니다.
2. "Add New" → "Project" → 방금 만든 GitHub 저장소를 선택(Import) 합니다.
3. Framework Preset은 "Other"(정적 HTML)로 두고, Build/Output 설정은 비워둔 채 그대로 "Deploy"를 누릅니다.
4. 배포가 끝나면 `https://프로젝트명.vercel.app` 형태의 주소가 생성됩니다.
5. 이후 GitHub 저장소에 수정 사항을 push할 때마다 Vercel이 자동으로 재배포합니다.
6. (선택) Vercel 프로젝트 설정 → Domains에서 보유 중인 도메인을 연결할 수 있습니다.

## 3. 폼프리(Formspree)로 신청 폼 연동하기

신청 폼(성함/연락처/자격사 등)이 접수되면 이메일로 알림을 받도록 연결하는 단계입니다.

1. formspree.io 에 접속해 회원가입합니다. (무료 플랜: 월 50건까지 무료)
2. "New Form"을 눌러 폼을 하나 생성하고, 알림 받을 이메일 주소를 등록합니다.
3. 생성된 endpoint 주소를 복사합니다. 형태: `https://formspree.io/f/xxxxxxxx`
4. `index.html`을 열어 아래 줄을 찾습니다.

   ```js
   const FORM_ENDPOINT = "https://formspree.io/f/여기에_발급받은_ID_입력";
   ```

5. `여기에_발급받은_ID_입력` 부분을 3번에서 복사한 실제 주소로 교체합니다.
   ```js
   const FORM_ENDPOINT = "https://formspree.io/f/xxxxxxxx";
   ```
6. 파일을 저장하고 GitHub에 다시 push하면 Vercel이 자동으로 재배포합니다.
7. 실제 배포된 페이지에서 신청서를 한 번 테스트 제출해보고, 등록한 이메일로 알림이 오는지 확인합니다.
   - 폼프리는 첫 제출 시 "이 폼을 승인해달라"는 확인 이메일을 보낼 수 있습니다. 이 경우 이메일의 확인 링크를 한 번 클릭해줘야 이후 제출부터 정상 수신됩니다.

## 요약 순서

1. GitHub 저장소 생성 → 파일 업로드/push
2. Vercel에서 해당 저장소 Import → Deploy → 배포 주소 확인
3. Formspree에서 폼 생성 → endpoint 발급 → `index.html`의 `FORM_ENDPOINT` 교체 → 다시 push
